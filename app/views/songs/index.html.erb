<ul>

<% @songs.each do |song| %>
  <li>
    <%= link_to song.title, song_path(song) %> - <%= display_artist song %>
    <%= link_to "Add Artist", edit_song_path(song) if song.artist == nil %>
  </li>
<% end %>
</ul>
<br>
<%= link_to "Add New Song", new_song_path %>
